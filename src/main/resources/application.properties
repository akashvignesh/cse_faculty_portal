spring.application.name=faculty-portal-backend

# ── Datasource (SSH tunnel must already be running: ssh -L 3307:oceanus:3306 asureshk@cerf.cse.buffalo.edu) ──
spring.datasource.url=jdbc:mysql://localhost:3307/ubs_emp?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=asureshk
spring.datasource.password=50608565
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.connection-timeout=30000

# ── JPA / Hibernate ──
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.open-in-view=false
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.orm.jdbc.bind=TRACE
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# ── Multipart upload limits ──
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=25MB